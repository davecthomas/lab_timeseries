---
agentmemory_version: "0.4.4"
timestamp: "2026-07-25T17:04:53Z"
author: "2355287-davecthomas"
branch: "main"
thread_id: "8f1e05c3-53f2-492e-aea8-e5a4b9c1f614"
turn_id: "27f65e7ed2"
workstream_id: "thread-8f1e05c3-53f2-492e-aea8-e5a4b9c1f614"
workstream_scope: "thread"
episode_id: "episode-main-a69c636cad"
episode_scope: "branch"
checkpoint_goal: "Make a fresh clone of the blood-metrics explorer runnable with a single command, continuing the onboarding effort that earlier fixed default-CSV resolution and rewrote the README."
checkpoint_surface: "Developer tooling and run-workflow surface: a new root Makefile in front of the Poetry-managed package, plus the dev-command sections of README.md and AGENTS.md."
checkpoint_outcome: "Added a self-installing Makefile (run/test/lint/check, PORT override, default goal runs the app) and repointed README and AGENTS.md dev commands at make, keeping raw Poetry as documented fallback."
decision_candidate: false
enriched: true
ai_generated: true
ai_model: "claude-fable-5[1m]"
ai_tool: "claude"
ai_surface: "claude-code"
ai_executor: "local-agent"
related_adrs:
files_touched:
  - ".codex/memory"
  - "AGENTS.md"
  - "Makefile"
  - "README.md"
design_docs_touched:
verification:
  - "git diff:  2 files changed, 12 insertions(+), 15 deletions(-); | Run app | `make` (installs deps, serves http://127.0.0.1:8050; `PORT=` to override) |; | Test | `make test` |; | Lint | `make lint` |"
source_pending_shards:
  - ".agents/memory/pending/2026-07-25/2026-07-25T17-04-53Z--2355287-davecthomas--thread_8f1e05c3-53f2-492e-aea8-e5a4b9c1f614--turn_27f65e7ed2.md"
---

## Why

- The documented workflow required knowing three separate Poetry commands before the app would start; contributors and agents each had to rediscover them. Standardizing on a Makefile whose default target installs dependencies and serves the ADR-0003 Dash explorer makes 'make' the canonical entry point for run, test, and lint, and both human docs (README) and agent docs (AGENTS.md) now teach the same commands.

## What changed

- A new root Makefile defines install, run, test, lint, check, and help targets; run depends on install so a bare 'make' on a fresh clone installs dev dependencies and serves the app on a PORT-overridable port. README's Install section collapsed into a make-first Run section with a one-line Poetry fallback, and AGENTS.md's dev-commands table was rewritten around make with Makefile added to its verified sources.

## Evidence

- The Makefile's run target declares install as a prerequisite and installs with dev dependencies, so the first-run bootstrap claim is verifiable directly from the target graph; its default goal is run, confirming bare 'make' serves the app. README now teaches 'make run PORT=8080' and 'make help' where it previously taught raw Poetry invocations, and AGENTS.md's repo-context block lists the Makefile among its verified sources, showing the agent-facing docs were re-verified against the new workflow rather than left stale.

## Next

- Makefile, AGENTS.md, and README changes are uncommitted on main; commit them from a branch per repo convention. CI still invokes ruff and pytest directly rather than the make targets, so the Makefile and CI steps can drift apart.
